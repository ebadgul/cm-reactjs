var ReactRouter = require('react-router')
var Router = ReactRouter.Router
var Route = ReactRouter.Route
var Link = ReactRouter.Link

var Main = React.createClass({
    render: function(){
        return(
            <p> this is main page yaaaaaaaay</p>
            );
    }
});

module.exports = Main;
